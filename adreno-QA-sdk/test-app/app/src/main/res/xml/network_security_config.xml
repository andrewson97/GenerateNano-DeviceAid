<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP for data ingestion endpoints (can be customized) -->
    <!-- Note: For production, use HTTPS endpoints -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
